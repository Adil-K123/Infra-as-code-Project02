name: "Terraform-Infra-Apply-StagingEnv"

on:
  push:
    branches:
      - staging
  #pull_request:
    #branches:
      #- staging

jobs:
  terraform:
    name: "Terraform"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Create symlink to modules directory
        run: ln -s ../modules ./staging/modules
        if: ${{ github.workspace }}/modules != './staging/modules'
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
            cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}
      - name: Terraform Init
        id: init
        run: terraform init
        working-directory: ./staging
      - name: Terraform Plan
        id: plan
        run: terraform plan
        working-directory: ./staging 
      - name: Terraform apply
        id: apply
        run: terraform apply --auto-approve
        working-directory: ./staging

